[project]
name = "security-agent-example"
version = "0.1.0"
description = "Security analysis agent built on deepagents"
requires-python = ">=3.11"
dependencies = [
    "deepagents>=0.5.0",
    "langchain>=1.2.0",
    "langchain-anthropic>=1.4.0",
    "httpx>=0.28.0",
    "python-dotenv>=1.0.0",
]

[tool.uv.sources]
deepagents = { path = "../../libs/deepagents", editable = true }
